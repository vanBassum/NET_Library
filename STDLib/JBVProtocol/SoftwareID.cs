using System;

namespace STDLib.JBVProtocol
{
    public enum SoftwareID : UInt32
    { 
        Unknown         = 0,
        Router          = 1,
        IDServer        = 2,
        TileMapClient   = 3,
        TileMapServer   = 4,
    }
}
